# Reproducible CPU environment for the tf215 Conda environment.
# Tested target: Python 3.9 on Linux/WSL without a visible CUDA device.
#
# The +cpu build prevents pip from downloading the CUDA 12 runtime packages.
--extra-index-url https://download.pytorch.org/whl/cpu

# Core numerical and surrogate-model stack
torch==2.8.0+cpu
numpy==1.24.3
pandas==2.3.3
scikit-learn==1.6.1
scipy==1.13.1
xgboost==2.1.4

# Bayesian optimisation and Gaussian processes
botorch==0.10.0
gpytorch==1.11

# Hyperparameter optimisation
optuna==4.9.0

# Training utilities and visualisation
tqdm==4.70.0
matplotlib==3.9.4
seaborn==0.13.2

# Data processing
openpyxl==3.1.5
joblib==1.5.3

# LLM/adapter tooling. Model weights and adapters are not installed here.
transformers==4.57.6
peft==0.17.1
accelerate==1.10.1
bitsandbytes==0.48.2 ; platform_system == "Linux" and platform_machine == "x86_64"
